---
kind: pipeline
name: start_drone
steps:
- commands:
  - echo start run drone
  image: alpine
  name: Start Run Drone
trigger:
  event:
  - push
type: docker
---
kind: pipeline
name: build
steps:
- commands:
  - echo start build on PR labeled
  image: alpine
  name: Build
trigger:
  action:
  - labeled
  event:
  - pull_request
type: docker
